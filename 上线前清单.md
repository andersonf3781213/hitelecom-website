# 上线前配置清单（Go-Live Checklist）

以下项目需要**贵司账号信息**才能完成，均为一次性配置（5–15 分钟/项）。配置位置全部集中在 `src/data/site.ts` 与 `public/admin/config.yml` 两个文件。

| # | 项目 | 状态 | 配置方法 |
|---|------|------|----------|
| 1 | 询盘表单真实发送（Contact / Partner / Service 三处表单） | ☐ 待配置 | 注册 web3forms.com（免费）→ 用 sales@hitelecom.com 获取 Access Key → 把 `site.ts` 的 `formEndpoint` 改为 `https://api.web3forms.com/submit?access_key=你的KEY` → 重新构建。**未配置时表单自动回退为 mailto: 唤起访客邮件客户端，功能不死链** |
| 2 | 高德地图（联系我们页） | ☐ 待配置 | lbs.amap.com → 应用管理 → 创建「Web端(JS API)」类型 Key（免费）→ 填入 `site.ts` 的 `amapKey` → 重新构建。**未配置时显示静态地址卡片，不报错** |
| 3 | CMS 在线后台（/admin/） | ☐ 待配置 | 把 `public/admin/config.yml` 里 `backend.repo: your-org/hitelecom-website` 改为真实 GitHub/Gitee 仓库，并按 Sveltia 文档配置 OAuth 或 Personal Access Token。详见《编辑指南.md》§1 |
| 4 | 社交媒体链接（Google 知识面板 sameAs） | ☐ 可选 | 若有 LinkedIn / YouTube / Facebook 企业主页，把 URL 加进 `src/layouts/BaseLayout.astro` Organization JSON-LD 的 `sameAs` 数组 |
| 5 | 统计代码（Google Analytics / Search Console） | ☐ 可选 | GSC 验证文件放 `public/` 根目录；GA4 代码片段加进 `BaseLayout.astro` 的 `<head>` |

## 已完成、无需配置的生产项

- ✅ 站点 URL / canonical / hreflang / sitemap（`astro.config.mjs` 已指向 `https://www.hitelecom.com`）
- ✅ robots.txt、llms.txt、favicon、OG 图（均在 `public/`，随构建原样输出）
- ✅ 询盘表单三页统一双模式提交 + 字段校验 + 中英感谢页（noindex 已处理）
- ✅ 19 个产品系列全部具备 PDF 规格书（`public/downloads/`，共 19 份）
- ✅ ICP 备案号与版权年份页脚
