# Hitelecom 官网 · 内容编辑指南

> 面向**非技术编辑**：日常更新不需要懂代码，两种入口任选——
> ① 网页后台 `/admin/`（推荐）；② 直接编辑 Markdown / 数据文件。
> 改完后网站会自动重新构建发布（取决于托管平台的 CI 配置）。

---

## 1. 后台登录（首次使用前由技术同事配置一次）

后台地址：**https://www.hitelecom.com/admin/**（Sveltia CMS）

配置文件 `public/admin/config.yml` 中有一行占位需要改成真实仓库：

```yaml
backend:
  name: github
  repo: your-org/hitelecom-website   # ← 改成实际 GitHub 仓库（组织/仓库名）
  branch: main
```

- 网站源码在 **GitHub** → 保持 `name: github`，编辑用 GitHub 账号登录；
- 在 **GitLab** → 改 `name: gitlab` 并补 `repo`、`branch`；
- 在 **Cloudflare Pages / Netlify 等无 Git 后端的环境** → 让技术同事接 `git-gateway`；
- 想先本地试用 → 临时改 `name: test-backend`（改动不会保存，仅体验界面）。

后台里能看到 4 个集合：**新闻·英文站 / 新闻·中文站 / 关于我们·英文站 / 关于我们·中文站**。

---

## 2. 发布一篇新闻（最常见的操作）

1. 后台 →「新闻 · 英文站」（或中文站）→ **New 新闻**；
2. 填写：
   - **标题**、**日期**（格式照原站：`hitelecom 2025.01.01`）；
   - **分类**：公司新闻 company / 展会信息 exh / 行业资讯 ind；
   - **列表卡片图**：上传后自动进入图片优化管线（自动生成 WebP、多尺寸）；
   - **网格位次**：填数字决定它在分类列表里的位置（0 = 第一个）；**头条轮播位次**同理（填了才会进顶部轮播）；
   - **正文**：可以直接**粘贴 HTML**（原站文章就是 HTML），也可以用编辑器排版；插图直接拖进去即可；
   - **相关推荐 / 上一篇下一篇**：填对应文章的 id（文件名，不带 .md）；
3. 保存 / 发布。**英文站和中文站是两个文件**（`src/content/news/en/<id>.md` 与 `zh/<id>.md`），双语文章请在两个集合各发一篇、**用同一个 id（文件名）**，这样语言切换才能互相对应。

> 小技巧：文章 id 建议延续数字编号（如 `1400`），与原站风格一致。

---

## 3. 修改「关于我们」

后台 →「关于我们 · 英文/中文站」→ 选板块（公司简介 / 品质保障 / 合作伙伴 / 加入我们 / 联系我们）→ 编辑 → 保存。
内容支持直接粘贴 HTML；图片拖入编辑器即可。

对应文件（手工编辑也可以）：

```
src/content/about/en/intro.md   quality.md  partner.md  joinus.md  contact.md
src/content/about/zh/intro.md   quality.md  partner.md  joinus.md  contact.md
```

---

## 3.1 上架一个新产品（CMS 新品，无需改代码）

后台 →「新品 · 英文站」（或中文站）→ **New 新品**，按表单填写：

- **Series 系列名**（如 `H300`）、**Tagline 一句话卖点**、**简介**（列表与详情页共用）；
- **列表卡片**：卡片名称、连接方式（如 `4G / NB-IoT / LoRa`）、卡片图（上传进 `src/assets/images/products`）；
- **详情页**：首屏大图（缺省用卡片图）、**PDF 规格书地址**（先把 PDF 放进 `public/downloads/`，再填 `/downloads/xxx.pdf`）、面包屑分类；
- **特性 / 规格表 / 应用场景**：逐条添加；规格表会自动进入网页表格和 AI 结构化数据；
- **GEO 增强字段**（强烈建议填）：`summary`（40–60 词直答摘要）、`applications`、`faqs`、`certifications`、`dateModified`——写法参照 `src/data/product.ts` 里 `details["282"]`（倾斜传感器样板）；

**文件名即网址**：保存为 `multi-gas-detector.md` → 上线后地址自动是 `/product/multi-gas-detector.html`（中文站在 `/zh/product/…`），并自动进入产品列表、全站搜索与 sitemap。双语同样请在英文/中文两个集合各发一篇、用同一文件名。

> 已有的 19 个内置系列仍在 `src/data/product.ts` 维护（见 §4）；CMS 新品与它自动合并，互不影响。

---

## 3.2 发布成功案例 / 技术博客

后台 →「成功案例 · 英文/中文站」或「技术博客 · 英文/中文站」→ New，字段与新闻相同（标题、日期、卡片图、正文、位次等）。

- **有任何条目时**，新闻中心会自动出现「Success Stories / Tech Blog」（中文：成功案例 / 技术博客）选项卡与列表页；
- **没有条目时**全站保持原样，不会出现空页面——放心按需发布；
- 双语站同样是两个集合各发一篇、用同一文件名。

---

## 4. 修改产品 / 解决方案 / 首页文案（给略懂代码的同事）

19 个内置产品系列与解决方案是**结构化数据**（规格表、特性图标、场景图互相关联），
直接在数据文件中维护，改错时构建会报错兜底，比 CMS 更安全（**全新产品建议走 §3.1 的 CMS 新品**）：

| 内容 | 文件 | 说明 |
|---|---|---|
| 产品列表 + 19 个系列详情 | `src/data/product.ts` | JSON 结构，字段含义见文件顶部注释；`specs` 即规格表（自动进入网页表格 + AI 结构化数据） |
| 解决方案 | `src/data/solution.ts` | 同上 |
| 首页各板块 | `src/data/home.ts` | 同上 |
| 公司信息 / 电话 / 邮箱 / 表单接口 | `src/data/site.ts` | 社媒链接、formEndpoint（询盘表单服务）都在这里 |
| 新闻 FAQ 手风琴 | `src/data/news.ts` 的 `staticConfig.faqs` | 6 条常见问题 |

**产品详情页的 AI 搜索增强字段**（GEO，建议每个系列都补）：
`summary`（40–60 词直答摘要，显示在首屏）、`applications`（应用领域，每条一句"测什么、解决什么问题"）、
`faqs`（3–5 条问答，正文与结构化数据同步）、`certifications`、`dateModified`。参照已完成的样板 `details["282"]`（倾斜传感器）。

---

## 5. 替换图片（最省事的方式）

所有网站图片在 `src/assets/images/` 下，**同名覆盖即可**，不用改任何代码：

- 想换某张图 → 找到同路径同名文件，用新图覆盖（保持文件名不变）→ 重新构建；
- 清晰度不用担心：构建只做格式转换（WebP）和必要缩放，不做激进压缩；
- 新闻正文插图、关于我们插图在 `src/assets/images/content/` 下。

---

## 6. 询盘表单（联系我们 / 合作伙伴页的留言）

`src/data/site.ts` 里的 `formEndpoint`：

- **已配置**（Web3Forms / Formspree 的接口地址）→ 访客提交后直接发邮件到 sales@hitelecom.com，并跳转感谢页（可用于转化统计）；
- **留空** → 自动回退为"打开访客自己的邮件客户端发信"，功能不缺失、无死链。

申请 Web3Forms（免费）：web3forms.com → 用 sales@hitelecom.com 注册拿 access key，
接口地址形如 `https://api.web3forms.com/submit`，填进 `site.ts` 即可。

---

## 7. 本地预览与发布

```bash
npm install        # 首次
npm run dev        # 本地预览 http://localhost:4321
npm run build      # 产出静态站到 dist/（部署即上传 dist 内容）
```

每次内容变更（后台保存 / 手工改文件）后，重新 `npm run build` 即完成更新；
接入托管平台 CI 后，推送到仓库会自动构建发布。
